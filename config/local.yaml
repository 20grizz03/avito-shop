 env: "local" #local, dev, prod
 http_server:
  address: "localhost:8080"
  timeout: "4s"
  idle_timeout: "60s"
  user: "admin"
 database:
  host: "localhost"
  port: 5432
  user: "postgres"
  name: "shop"
 jwt:
  token_ttl: 60
 migrations:
  path: "./migrations"